@model IList<CategoryModel>
@if (Model.Count > 0)
{
    <section class="container">
        <div class="container">
            <div class="d-sm-flex align-items-center justify-content-between mb-4 pb-2">
                <h2 class="h3 mb-sm-0">Kategorije</h2>
                <a class="btn btn-link fw-normal ms-sm-3 p-0" href="city-guide-catalog.html">
                    View all<i class="fi-arrow-long-right ms-2"></i>
                </a>
            </div>
        </div>
        <div class="container py-5 pt-xxl-4 mt-md-2 mb-md-4">
            <div class="row row-cols-lg-6 row-cols-sm-3 row-cols-2 g-3 g-xl-4">
                @foreach (var item in Model)
                {
                    <div class="col">
                        <a style="background-image: url(&quot;@item.PictureModel.ImageUrl&quot;)" class="icon-box card card-body h-100 border-0 shadow-sm card-hover text-center" href="@Url.RouteUrl("Category", new {SeName = item.SeName})" title="@item.PictureModel.Title">
                            @*<div class="icon-box-media bg-faded-accent text-accent rounded-circle mb-3 mx-auto">
                            <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title"/>
                        </div>*@
                            <h3 class="icon-box-title fs-base mb-0">@item.Name</h3>
                        </a>
                    </div>
                }
            </div>
        </div>
    </section>
    @*<div class="category-grid home-page-category-grid">
        <div class="item-grid">
            @foreach (var item in Model)
            {
                <div class="item-box">
                    <div class="category-item">
                        <h2 class="title">
                            <a href="@Url.RouteUrl("Category", new {SeName = item.SeName})" title="@item.PictureModel.Title">
                                @item.Name
                            </a>
                        </h2>
                        <div class="picture">
                            <a href="@Url.RouteUrl("Category", new {SeName = item.SeName})" title="@item.PictureModel.Title">
                                <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title"/>
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>*@
}